[install]
# Only resolve package versions published at least this many seconds ago (7 days).
minimumReleaseAge = 604800

[run]
# Run package scripts with the Bun runtime (needed for marp-cli; avoids Node+CJS edge cases).
bun = true
